# sqlalchemy-dev (Antigravity plugin)

SQLAlchemy dev plugin for Agents Store. Model definition patterns, relationship mapping, query optimization, Alembic migrations, and troubleshooting for developers building with SQLAlchemy 2.0+.

## Install

Workspace-scoped:
```bash
agy plugin install ./sqlalchemy-dev
```
Copies this directory to `.agents/plugins/sqlalchemy-dev/` in the current workspace.

Global:
```bash
agy plugin install --global ./sqlalchemy-dev
```
Copies this directory to `~/.gemini/config/plugins/sqlalchemy-dev/` instead.

## Workflows (UNVERIFIED)

The `workflows/*.md` directory convention is **not confirmed by official Antigravity documentation** as of 2026-08-17. If workflows aren't picked up automatically after install, paste their content manually via the editor's "+ Workspace" button.

## Source

Canonical: https://github.com/agents-store/claude-public-plugins/tree/main/plugins/sqlalchemy-dev
