{
  "mcpServers": {
    "drawio": {
      "serverUrl": "https://mcp.draw.io/mcp"
    },
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--headless"
      ]
    }
  }
}